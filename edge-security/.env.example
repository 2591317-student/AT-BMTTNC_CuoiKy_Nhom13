# Shared keys — dùng chung cho API server và Sensor simulator
# Sinh key mới: python -c "import secrets; print(secrets.token_hex(32))"

API_KEY=your-api-key-here
HMAC_SECRET=your-hmac-secret-here
AES_KEY=your-aes-key-here
